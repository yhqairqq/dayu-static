import React from 'react';

class ReportQuery extends React.Component {
  componentDidMount() {
    // const { match: { params } } = this.props;
    // const { reportId } = params;
  }

  render() {
    return <span>报表查询</span>;
  }
}

export default ReportQuery;
